# Dependencies for the Vercel deployment (entrypoint: api/index.py).
# No TensorFlow or Celery here - models run with numpy, predictions run in the request.
# The full local stack (TensorFlow, Celery, Redis) is in Django/requirements.txt.
Django>=4.2.7,<5.0
numpy>=1.26,<3
pandas>=2.2.3,<3
requests>=2.31.0
python-dotenv>=1.0.0
whitenoise>=6.6.0,<7
dj-database-url>=2.1.0
psycopg2-binary>=2.9.10
